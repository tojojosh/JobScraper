# ─── UK Skilled Jobs Portal – Environment Variables ───

# Secret key for Flask sessions (change in production!)
SECRET_KEY=your-secret-key-here

# ─── Scraper Schedule (24h format, UTC) ───
SCRAPE_HOUR=6
SCRAPE_MINUTE=0

# ─── Rate Limiting (seconds between requests) ───
REQUEST_DELAY_MIN=1.5
REQUEST_DELAY_MAX=4.0
MAX_PAGES_PER_SOURCE=5
MAX_RESULTS_PER_COMPANY=50

# ─── Adzuna API (free – register at https://developer.adzuna.com/) ───
ADZUNA_APP_ID=
ADZUNA_API_KEY=

# ─── Reed API (free – register at https://www.reed.co.uk/developers) ───
REED_API_KEY=

# ─── Database (default: SQLite in data/jobs.db) ───
# DATABASE_URL=sqlite:///data/jobs.db
